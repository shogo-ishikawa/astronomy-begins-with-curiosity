# ABCsをCodexで実装するときの依頼文

## 推奨する使い方

仕様書全文をチャットへ毎回貼り付ける必要はありません。次の2ファイルをリポジトリへ置き、各依頼では「今回のフェーズ」と「完成条件」だけを短く指定します。

- `AGENTS.md`: Codexが作業時に従う常設ルール
- `docs/ABCs_v0.1_implementation_spec.md`: v0.1の仕様上の正本

最初から全フェーズを一括で依頼せず、Phase 0から順番に、各フェーズを「実装 → 検証 → 人が確認 → 次へ」の単位で進めてください。

## 最初の依頼文（そのまま使用可能）

```text
このリポジトリで、教育用Webアプリ
「ABCs — Astronomy Begins with Curiosity」のv0.1開発を始めてください。

最初に、リポジトリ直下の AGENTS.md と
docs/ABCs_v0.1_implementation_spec.md を全文読み、目的、非目標、
科学的制約、完成条件を把握してください。矛盾や実装を開始できない不足があれば、
コードを変更する前に具体的に報告してください。

今回は仕様書の「Phase 0: 基盤」だけを実装してください。
Phase 1以降の画面や教材内容は先行実装しないでください。

進め方:
1. 現在のリポジトリ構成と既存変更を確認する。
2. Phase 0の実装計画と、変更予定ファイルを簡潔に示す。
3. Vite + React + TypeScript strictを基盤として実装する。
4. 空の研究プロジェクトを作成、IndexedDBへ保存、一覧表示、再開できるようにする。
5. theme/contentの検証可能なschema、ProjectStateの初期版、GitHub Pages向けbuild、
   CIと必要なテストを用意する。
6. format、lint、typecheck、test、production buildを実行する。
7. 実装内容、変更ファイル、検証結果、未確定事項を報告して停止する。

追加条件:
- 日本語UIにする。
- GitHub Pagesだけで動作し、backend、APIキー、account、生成AIを追加しない。
- v0.1ではService Workerを登録しない。
- 教材本文をReact componentへ大量に直書きしない。
- アクセシビリティと科学的provenanceを、後付けではなくデータ設計へ含める。
- 依存追加には理由を示す。
- 明示的に依頼するまでcommit、push、deploy、PR作成は行わない。
```

## 各フェーズの依頼テンプレート

Phase 1以降は、次のテンプレートの角括弧だけを置き換えて使います。

```text
AGENTS.md と docs/ABCs_v0.1_implementation_spec.md を全文読み、
既存実装と直前フェーズの検証結果を確認してください。

今回は「[Phase番号と名称]」だけを実装してください。
仕様書にあるこのフェーズの完了基準と関連する完成条件を満たし、
後続フェーズは先行実装しないでください。

まず実装計画を示し、その後に実装とテストを行ってください。
完了時には、利用可能になったこと、変更ファイル、実行した検証と結果、
残る仮定・課題、次に進めるフェーズを報告してください。
commit、push、deploy、PR作成は行わないでください。
```

## フェーズごとの短い追加指示

### Phase 1: 研究への招待と計画

```text
Mira、クリック可能な用語解説、学問とのつながり、研究への招待、
問い・仮説・予想、研究計画、Miraレビューを実装してください。
一本道にせず、選択結果と改善理由が画面と研究記録へ一貫して反映されることを
テストしてください。
```

### Phase 2: 試し計算とデータ

```text
ResultPackage schema、明示ラベル付きdemo fixture、派生量、必須の一変数比較、
PlanVersion、品質確認を実装してください。
fixtureをCWSデータとして表示せず、設定・単位・provenanceを常に確認できるように
してください。
```

### Phase 3: GUI解析と図

```text
AnalysisRecipe、TypeScriptの科学計算、密度図、密度ヒストグラム、
成長図、図の保存とprovenanceを実装してください。
表示スケールで見え方が変わっても基礎データが変化しないことをテストしてください。
```

### Phase 4: Python支援

```text
PyodideをWorkerで遅延ロードし、段階的コードテンプレートとMiraの支援を実装してください。
停止・再起動・GUIへの安全な復帰を可能にし、GUIとPythonの主要数値が仕様の許容誤差内で
一致することをテストしてください。
```

### Phase 5: 解釈とミニ論文

```text
結果、解釈、結論、限界を分けた選択、過剰解釈のレビュー、ミニ論文、
Markdown・印刷・JSON出力、Research Constellationを実装してください。
学生の必須判断をMiraが代行しないことを確認してください。
```

### Phase 6: 品質仕上げ

```text
アクセシビリティ、性能、E2E、教材校閲、GitHub Pages用workflowを仕上げてください。
仕様書の完成条件20項目を一つずつ検証し、合否と根拠を一覧で報告してください。
deployは行わず、実行可能な状態までにしてください。
```

## 仕様変更が必要になったときの依頼文

実装中に仕様の変更が必要になった場合は、コードと仕様を暗黙にずらさず、先に判断を分けます。

```text
実装は一旦止め、見つかった仕様上の論点について、
1. 現在の仕様
2. 問題になる理由
3. 選択肢とそれぞれの教育・科学・実装上の影響
4. 推奨案
を示してください。承認するまでは仕様書とコードを変更しないでください。
```
